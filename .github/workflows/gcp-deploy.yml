name: Deploy Backend to Goggle App Engine

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment: Production

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Setup Node.js and yarn
        uses: actions/setup-node@v2
        with:
          node-version: "22"

      - name: Install dependencies
        working-directory: ./backend
        run: yarn install --frozen-lockfile

      - name: Build Node Project
        working-directory: ./backend
        run: yarn build
        run: |
          yarn build

          touch ./dist/.env
          echo SERVICE_PORT=${{ vars.SERVICE_PORT }} >> ./dist/.env
          echo SERVICE_HOST=${{ vars.SERVICE_HOST }} >> ./dist/.env
          echo SERVICE_NAME=${{ vars.SERVICE_NAME }} >> ./dist/.env
          echo BUILD_DOCS=${{ vars.BUILD_DOCS }} >> ./dist/.env
          echo JWT_EXPIRATION_TIME=${{ vars.JWT_EXPIRATION_TIME }} >> ./dist/.env
          echo CORS_ORIGIN=${{ vars.CORS_ORIGIN }} >> ./dist/.env
          echo CORS_METHODS=${{ vars.CORS_METHODS }} >> ./dist/.env

          echo DB_DATABASE=${{ secrets.DB_DATABASE }} >> ./dist/.env
          echo DB_HOST=${{ secrets.DB_HOST }} >> .env
          echo DB_PASSWORD=${{ secrets.DB_PASSWORD }} >> ./dist/.env
          echo DB_PORT=${{ secrets.DB_PORT }} >> ./dist/.env
          echo DB_USERNAME=${{ secrets.DB_USERNAME }} >> ./dist/.env
          echo JWT_SECRET_KEY=${{ secrets.JWT_SECRET_KEY }} >> ./dist/.env

      - name: Google Cloud Auth
        uses: "google-github-actions/auth@v2"
        with:
          credentials_json: "${{ secrets.GCP_SA_KEY }}"
          project_id: ${{ secrets.GCP_PROJECT_ID }}

      - name: Set up Cloud SDK
        uses: "google-github-actions/setup-gcloud@v2"

      - name: Deploy to Google App Engine
        working-directory: ./backend
        run: "gcloud app deploy app.yaml --quiet"
