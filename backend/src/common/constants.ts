export const USERNAME_MIN_LENGTH = 3;
export const USERNAME_MAX_LENGTH = 16;
export const USERNAME_PATTERN = /^[a-zA-Z0-9]{3,16}$/;

export const VERIFIER_PATTERN = /^[a-f0-9]{512}$/;
export const SALT_PATTERN = /^[a-f0-9]{64}$/;
export const PUBLIC_KEY_PATTERN = /^[a-f0-9]{512}$/;
export const PROOF_PATTERN = /^[a-f0-9]{64}$/;
